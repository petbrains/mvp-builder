# Setup - Monthly Summary

## Install
```bash
# No additional backend dependencies

# No additional frontend dependencies
# Uses Tailwind for simple bar visualization
```

## Config
```env
# No additional configuration needed
```

## Run
```bash
# Same as previous features
cd backend && npm run dev
cd frontend && npm run dev
```

## Test
```bash
# Backend summary tests
cd backend && npm test -- --watch summary

# Frontend summary tests
cd frontend && npm test -- --watch Summary
```
